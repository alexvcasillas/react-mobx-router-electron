import React, { PropTypes } from 'react';

class User extends React.Component {
  render() {
    return (
      <div>
        This is the User Container
      </div>
    );
  }
}

export default User;
